import { FilterParamsPipe } from './filter-params.pipe';

describe('FilterParamsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterParamsPipe();
    expect(pipe).toBeTruthy();
  });
});
